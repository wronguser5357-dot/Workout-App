# Mockups

Drop visual mockups here (PNG, JPG, SVG, or Figma exports). Name files so the intent is obvious, e.g.:

- `01-home.png`
- `02-workout-detail.png`
- `03-settings-mobile.png`

If a mockup has multiple states (hover, empty, error), include the state in the filename.
