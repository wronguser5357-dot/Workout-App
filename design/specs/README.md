# Specs

Drop written specifications here. Suggested files:

- `overview.md` — product goal, users, core flows.
- `screens.md` — per-screen behavior, states, edge cases.
- `tokens.md` — colors, typography, spacing, radius, shadow values.

Claude Code will read these before touching `src/`.
