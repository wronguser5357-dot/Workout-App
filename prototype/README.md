# Prototype

Place the HTML/CSS/JS prototype from Claude Design here — exactly as exported, unmodified.

Claude Code treats this folder as reference, not production. Structural patterns, class names, and token values can be lifted into `src/`, but the final implementation should be rewritten for clarity, accessibility, and maintainability.
