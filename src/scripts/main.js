// Workout — entry script
// Keep modules small; split by feature as the app grows.

document.addEventListener("DOMContentLoaded", () => {
  // TODO: mount initial UI / wire up interactions per design specs.
});
